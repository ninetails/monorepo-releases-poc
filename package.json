{
  "name": "monorepo-releases-poc",
  "private": true,
  "engines": {
    "node": ">=12"
  },
  "repository": "ninetails/monorepo-releases-poc",
  "homepage": "https://github.com/ninetails/monorepo-releases-poc",
  "license": "MIT",
  "devDependencies": {
    "@commitlint/cli": "^9.0.1",
    "@commitlint/config-conventional": "^9.0.1",
    "husky": "^4.2.5",
    "lerna": "^3.22.1",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "clean": "rimraf node_modules package-lock.json yarn.lock |:",
    "reset": "$npm_execpath run clean && $npm_execpath install"
  }
}
